# Task Manager API

Task Manager — это сервис для управления задачами, реализованный на базе **FastAPI**.  
Поддерживает операции CRUD (создание, чтение, обновление и удаление задач).

---

## Возможности API

### 🔹 Получение задачи
```http
GET /api/v1/task
Без параметров возвращает последнюю созданную задачу

С параметром uuid — задачу с конкретным UUID

Параметры:

uuid (query, optional) — уникальный идентификатор задачи

Коды ответов:

200 OK — задача найдена

404 Not Found — задача отсутствует

🔹 Получение всех задач
http
Копировать
Редактировать
GET /api/v1/task/all
Возвращает список всех задач.

Коды ответов:

200 OK — список задач

404 Not Found — задач в базе нет

🔹 Создание задачи
h
Копировать
Редактировать
POST /api/v1/task
Пример запроса:

json
Копировать
Редактировать
{
  "uuid": "d290f1ee-6c54-4b01-90e6-d701748f0851",
  "title": "Новая задача",
  "description": "Описание задачи",
  "status": "CREATE"
}
Коды ответов:

200 OK — задача создана

500 Internal Server Error — ошибка при сохранении

🔹 Обновление задачи
http
Копировать
Редактировать
PATCH /api/v1/task/{uuid}
Пример запроса:

json
Копировать
Редактировать
{
  "title": "Обновлённая задача",
  "status": "IN_PROGRESS"
}
Коды ответов:

200 OK — задача успешно обновлена

404 Not Found — задача с таким UUID не найдена

🔹 Удаление задачи
http
Копировать
Редактировать
DELETE /api/v1/task/{uuid}
Коды ответов:

200 OK — задача удалена

404 Not Found — задача с таким UUID не найдена

⚙️ Технологии
FastAPI — быстрый web-фреймворк для Python

SQLModel — ORM и работа с базой

Pydantic — валидация данных

Alembic — миграции базы данных

Pre-commit + Ruff + MyPy — линтинг и статический анализ

▶️ Запуск проекта
Клонировать репозиторий:

bash
Копировать
Редактировать
git clone https://github.com/kintoa/task-manager.git
cd task-manager
Установить зависимости:

bash
Копировать
Редактировать
pip install -r requirements.txt
Применить миграции:

bash
Копировать
Редактировать
alembic upgrade head
Запустить сервер:

bash
Копировать
Редактировать
uvicorn src.main:app --reload
Открыть документацию:

Swagger UI: http://localhost:8000/docs

ReDoc: http://localhost:8000/redoc

✅ Тестирование
Запуск тестов:

bash
Копировать
Редактировать
pytest